import type { NextPage } from "next";
import EventCalendar from '@/components/blocks/Calendar';

const Calendar: NextPage = () => {
  return (
    <EventCalendar />
  );
};

export default Calendar;
